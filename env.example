# Copy to .env in this repo root (never commit .env):
#   cp env.example .env
# python-dotenv loads .env when running scripts/reclassify_holds.py

# Required for `python scripts/reclassify_holds.py classify` (xAI API).
# https://console.x.ai/
XAI_API_KEY=
